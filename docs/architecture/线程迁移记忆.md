# 线程迁移记忆

Updated: 2026-04-17

## 目的

这份文档用于把旧线程里积累的项目上下文，完整迁移到 `E:\genergi`。  
如果未来新开线程、切换终端、或侧边栏仍显示旧目录，都以本文件和 `E:\genergi\AGENTS.md` 为准。

## 当前项目唯一母体

- 当前真实项目根目录：`E:\genergi`
- 旧目录 `E:\short-video-factory`：仅作参考源，不再作为母体开发

## 产品身份

- 主品牌：`GENERGI`
- 产品展示名：`GENERGI 自动化视频平台`
- 公司 / 域名层：`Genergius`
- 主入口域名：`ai.genergius.com`

## 产品方向

- 纯线上 Web 平台，不做 Electron 软件形态
- 中文后台，英语内容输出
- 面向海外英语国家用户
- 默认平台：TikTok / Reels / YouTube Shorts

## 技术架构

- Monorepo 根：`E:\genergi`
- `apps/web`：React + Vite
- `apps/api`：Hono
- `apps/worker`：Node.js + BullMQ
- `packages/shared`：共享 contracts / persistence
- `packages/config`：模式 / 品牌 / 渠道 / provider 配置
- `packages/ui`：设计系统基础

## 已完成的关键能力

### 平台与部署

- `https://ai.genergius.com` 已上线
- HTTPS 已启用
- 正式部署模型：
  - `release/current/current.prev`
  - `systemd`
  - `nginx`
- 可复用部署能力已开始工程化：
  - `pnpm deploy:production`
  - `pnpm deploy:hotfix`

### 核心产品能力

- 登录页已存在
- 任务启动工作台已存在
- 分镜审阅页已存在
- 关键帧审阅页已存在
- 批量任务看板已存在
- 素材资产中心已存在
- 用户中心已存在

### 真实媒体链

- Edge TTS 已真实产出：
  - `mp3`
  - `srt`
- 第一家视频中转站已真实产出：
  - 原始 `scene-1.mp4`
- 最终成片已真实产出：
  - `final-with-audio.mp4`
- 资产中心已支持：
  - 元数据展示
  - 下载
  - 预览接口

## 文本 provider 现状

### 已支持的协议

- `anthropic-native`
- `openai-compatible`

### 第二家文本中转站

- 新 provider 接入已实测可用
- 当前线上已切到：
  - `GENERGI_TEXT_PROVIDER=anthropic-native`
  - `GENERGI_TEXT_BASE_URL=<private-by-env>`
  - `GENERGI_TEXT_MODEL=claude-opus-4-6`

说明：
- 文本协议层已经打通
- 私有 endpoint 不进入公开仓库，统一通过部署环境变量注入
- 未来更换为 OpenAI-compatible 中转站时，不需要改 worker 主逻辑，只改环境变量

## 媒体链现状

### 已稳定

- 视频生成
- TTS 音频
- 字幕
- 最终成片
- 资产下载

### 仍在持续优化

- 服务器侧直接图片生成稳定性
- 当前已经有 fallback：
  - 图片网关失败时，从真实视频抽帧生成关键帧包

也就是说：
- 平台已经能完整生成任务结果
- 但“图片直出”仍值得继续优化

## 用户中心现状

### 已完成

- 用户持久化存储
- 用户列表
- 新建用户
- 编辑昵称 / 账号
- 启用 / 停用
- 重置密码
- 记住密码（浏览器本地）

### 已通过的真实验收

- 新建用户成功
- 停用用户无法登录
- 重新启用后可以用新密码登录
- 接口不再回传 `passwordHash`

## 设计基线

- UI source of truth：
  - `docs/architecture/ui-baseline-manifest.md`
  - `docs/architecture/ui-direction.md`
  - `DESIGN.md`

说明：
- `DESIGN.md` 放在仓库根目录，作为全局 UI 设计契约

## 继续开发时的优先级建议

如果未来从新线程继续接手，默认优先级是：

1. 稳定图片直出链
2. 持续增强用户中心与登录体验
3. 持续增强部署可靠性
4. 继续把资产中心做成更完整的预览 / 导出工作台

## 重要纪律

- 不要再把 `E:\short-video-factory` 当作主项目继续改
- 不要把真实 key、密码、token 写进仓库
- 新线程若显示旧目录，也要以 `E:\genergi` 为真实项目根
